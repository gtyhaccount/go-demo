package遵循以下原则：

package是最基本的分发单位和工程管理中依赖关系的体现
每个Go语言源代码文件开头都必须要有一个package声明，表示源代码文件所属包
要生成Go语言可执行程序，必须要有名为main的package包，且在该包下必须有且只有一个main函数

*同一个目录下只能存在一个package，一个package可以由多个源代码文件组成

*import 的是目录，调用时用包名

*package main里面的方法或类不能被其它package下的程序引用！